# Server
PORT=8080

# Redis (leave password empty for local Redis without auth)
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=
REDIS_DB=0

# Optional: full URL (Render Key Value sets this automatically)
# REDIS_URL=redis://localhost:6379/0
